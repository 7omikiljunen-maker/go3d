{
  "rules": {
    "rooms": {
      "$roomId": {
        ".read": true,
        ".write": "auth != null && ((!data.exists() && root.child('users').child(auth.uid).child('paid').val() === true && newData.child('hostUid').val() === auth.uid) || (data.exists() && data.child('hostUid').val() === auth.uid) || (data.exists() && data.child('guestUid').val() === auth.uid) || (data.exists() && !data.child('guestUid').exists() && newData.child('guestUid').val() === auth.uid && newData.child('hostUid').val() === data.child('hostUid').val()))",
        "chat": {
          "$msgId": {
            ".validate": "!newData.exists() || (newData.hasChildren(['player','text','ts']) && newData.child('text').isString() && newData.child('text').val().length > 0 && newData.child('text').val().length <= 500)"
          }
        }
      }
    },
    "users": {
      "$uid": {
        ".read":  "auth != null && auth.uid === $uid",
        ".write": false
      }
    },
    "payments": {
      ".read":  false,
      ".write": false
    }
  }
}
